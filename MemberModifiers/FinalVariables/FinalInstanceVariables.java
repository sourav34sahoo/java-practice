class FinalInstanceVariables
{
	int x;
}

/*For the normal instance variables it is not required to perform initialization 
explicitly JVM will always provide default values.*/
  
class FinalInstanceVariables
{
	final int x;
}

/*if the instance variable declare as the final then compulsary we should perform  
initialization wheather we are using or not otherwise we will get C.E .*/