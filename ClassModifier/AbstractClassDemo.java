abstract class AbstractClassDemo
{
	
}
AbstractClassDemo ACD = new AbstractClassDemo();
 
 // if any java class if we don't want instanciation then we have to declare that class as abstract i.e for abstract classes instanciation (creation of object) is not possible  
 